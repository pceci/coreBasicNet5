@model coreBasicNet5.Entities.cRol

@using (Html.BeginForm("RolDeleteConfirmed", "Admin", FormMethod.Post))
{
    @Html.Partial("_RolDetails", Model)
  <h5>¿Desea eliminar?</h5>
  <div class="col-md-8">
    <button id="submit" name="submit" class="btn btn-primary" value="1">Si</button>
    <a href="/Admin/RolIndex/" id="cancel" class="btn btn-default">No</a>
  </div>
}